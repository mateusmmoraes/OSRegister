@model IEnumerable<OSRegister.Operator>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OperatorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OperatorRole)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Service)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OperatorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OperatorRole)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Service.ServiceId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OperatorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OperatorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OperatorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
